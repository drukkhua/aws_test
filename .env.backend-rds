# Django Configuration
DEBUG=False
SECRET_KEY=your-secret-key-here-change-in-production
DJANGO_SETTINGS_MODULE=backend_rds.custom_settings

# Database Configuration
DB_NAME=f1store
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

# Database URL (alternative to individual DB variables)
DATABASE_URL=postgresql://postgres:postgres@db:5432/f1store

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:80,http://localhost:3000
